# Create a Unix domain socket server

Create a Unix domain socket server

## Usage

``` r
ipc_create_server(path)
```

## Arguments

- path:

  Character string, the file path for the socket

## Value

A server connection object from processx
