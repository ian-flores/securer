# Windows-supported resource limit names

Returns the limit names that can be enforced on Windows via Job Objects.

## Usage

``` r
windows_supported_limits()
```

## Value

A character vector of supported limit names.
