% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rlimits.R
\name{generate_ulimit_commands}
\alias{generate_ulimit_commands}
\title{Generate ulimit shell commands from a limits list}
\usage{
generate_ulimit_commands(limits)
}
\arguments{
\item{limits}{A named list of resource limits, or \code{NULL} for no limits.}
}
\value{
A character vector of shell commands (one per limit), or
\code{character(0)} if \code{limits} is \code{NULL} or empty.
}
\description{
Translates a user-facing limits list into shell \code{ulimit} commands that
can be injected into wrapper scripts before the \code{exec} line.
}
\details{
Supported limit names:
\describe{
\item{cpu}{CPU time in seconds (\code{ulimit -t})}
\item{memory}{Virtual memory (address space) in bytes (\code{ulimit -v}).
Converted to kilobytes for ulimit.}
\item{fsize}{Maximum file size in bytes (\code{ulimit -f}).
Converted to 512-byte blocks for ulimit.}
\item{nproc}{Maximum number of processes (\code{ulimit -u})}
\item{nofile}{Maximum number of open files (\code{ulimit -n})}
\item{stack}{Maximum stack size in bytes (\code{ulimit -s}).
Converted to kilobytes for ulimit.}
}
}
\keyword{internal}
